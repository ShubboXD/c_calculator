compile main.c using "gcc -o main main.c -lm" in ur terminal open on the souce folder,
you can replace main with the name you want,
you can ignore -lm,
you can ignore the already compiled "main" file,